spring:
  datasource:
    url: jdbc:mysql://database-auth:3306/lms-auth?createDatabaseIfNotExist=true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://backend-discovery-server:8761/eureka
