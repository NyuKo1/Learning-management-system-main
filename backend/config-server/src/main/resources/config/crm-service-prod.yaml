spring:
  datasource:
    url: jdbc:mysql://database-crm:3306/lms-crm?createDatabaseIfNotExist=true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://backend-discovery-server:8761/eureka
