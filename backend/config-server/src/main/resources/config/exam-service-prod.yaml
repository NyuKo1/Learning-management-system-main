spring:
  datasource:
    url: jdbc:mysql://database-exam:3306/lms-exam?createDatabaseIfNotExist=true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://backend-discovery-server:8761/eureka
