spring:
  datasource:
    url: jdbc:mysql://database-notify:3306/lms-notify?createDatabaseIfNotExist=true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://backend-discovery-server:8761/eureka
